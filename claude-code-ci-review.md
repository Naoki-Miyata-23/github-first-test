# GitHub CI上でのClaude Code AIレビュー導入検討資料

## 1. 概要

GitHub ActionsにClaude Code公式の連携機能(`claude-code-action`)を組み込むことで、Pull Request作成時にAIが自動でコードレビューを行い、レビューコメントを残す仕組みを構築できる。

- 対象: 個人アカウントでも、組織アカウントでも利用可能(ただし機能に差がある。詳細は4章)
- できること: 変更内容の指摘・コメント投稿、`@claude`メンションへの応答、指定イベントでの自動実行
- **できないこと: マージの可否を自動でブロックすることはできない。**あくまで人間の判断を補助する位置づけ

## 2. セットアップ方法

### 2-1. 簡易セットアップ(個人・小規模向け)

Claude Code上で `/install-github-app` コマンドを実行すると、以下が自動化される。

- GitHub Appのインストール
- APIキー(シークレット)のリポジトリへの登録
- `.github/workflows/` へのワークフローファイル追加(PRとして提示される)

要件: ローカルにGitHub CLI(`gh`)がインストール済みで、対象リポジトリの管理者権限を持っていること。

### 2-2. 手動セットアップ

1. https://github.com/apps/claude をインストール
2. リポジトリの Settings → Secrets and variables → Actions に認証情報を登録
   - `ANTHROPIC_API_KEY`(APIキー、従量課金)、または
   - `CLAUDE_CODE_OAUTH_TOKEN`(`claude setup-token` で発行。Claudeサブスクリプションの利用枠を消費)
3. `.github/workflows/` にワークフローYAMLを追加

```yaml
name: Claude Code
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
jobs:
  claude:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```

## 3. 動作モード

| モード | トリガー | 挙動 |
|---|---|---|
| インタラクティブ | PR/Issueへの `@claude` メンション | コメントで応答、コミット・プッシュも可能 |
| オートメーション | 任意のGitHubイベント(push, pull_request, schedule等) | メンション不要で自動実行、結果はワークフローログやコメントに出力 |

## 4. 個人利用 vs 組織利用の違い

| | GitHub Action(本資料の対象) | 公式「Code Review」サービス |
|---|---|---|
| 対象プラン | 個人プランでも利用可 | **Team/Enterpriseプラン限定** |
| セットアップ | `.github/workflows/` にYAML追加(自前で構築) | 組織管理者が管理画面から有効化するだけ |
| 実行タイミング | 自由に設定可能(push毎、PR作成時など) | PR作成時・push毎に自動 |
| カスタマイズ性 | 高い(トリガー条件、対象範囲を自由に設計) | `CLAUDE.md` / `REVIEW.md` でのカスタマイズのみ |
| 料金体系 | 下記5章参照(2通り) | 常に別課金(利用クレジット消費) |

大規模開発・組織導入を検討する場合は、両方が選択肢に入る。GitHub Actionは柔軟だが構築・運用の手間がかかり、Code Reviewサービスは手軽だがTeam/Enterprise契約と追加コストが前提になる。

## 5. 料金体系

CI上でのAIレビューには、以下2種類の費用が発生しうる。

### 5-1. GitHub Actionsの実行時間(GitHub側)

- Publicリポジトリ: 無料(無制限)
- Privateリポジトリ: 月間無料枠を超えると従量課金

### 5-2. Claude APIのトークン利用料(Anthropic側)

認証方法によって、今契約している利用枠との関係が変わる。

| 認証方法 | 課金の扱い |
|---|---|
| `ANTHROPIC_API_KEY` | **完全に別会計。** Anthropic Consoleでの従量課金。既存のサブスクリプション利用枠とは無関係に追加費用が発生 |
| `CLAUDE_CODE_OAUTH_TOKEN` | **既存の利用枠を消費。** 追加費用は発生しないが、Claude Codeの対話利用と同じ枠を共有するため、CI利用が増えると開発者本人の利用可能量を圧迫する |

## 6. 料金の目安

参考値として、Anthropic公式「Code Review」サービスは **1回のレビューあたり約$15〜25(約2,200円〜3,600円)** とされる(PRの規模・複雑さにより変動)。GitHub Action自前構築の場合はより安価に抑えられる可能性があるが、PRの規模・頻度・対象コードベースの大きさに応じて増減する。

### 大規模開発でのシミュレーション例

```
1日20件のPRをレビュー × $20(中間値) = $400/日
月20営業日換算 → 約$8,000/月(約120万円/月)
```

Claude Opus 5のAPI単価(参考、2026年8月時点):
- 入力: $5.00 / 100万トークン
- 出力: $25.00 / 100万トークン

Anthropic公式が示す実測ベースの目安:
- エンジニア1人あたり平均 **1日あたり約$13**
- 利用者の90%は **1日あたり$30未満** に収まる
- 積極的に使うチームで **月$150〜250/人** 程度のレンジが目安

## 7. レビュー対象の範囲とトークン使用量の実態

「毎回リポジトリ全体を読み込んでいるのでは」という懸念があるが、実際には以下の通り。

| 項目 | 実態 |
|---|---|
| デフォルトの挙動 | Claudeは自動でリポジトリ全体を読み込むのではなく、**PRの差分(diff)や変更ファイルを自発的に取得して**レビューする。トークン使用量は基本的に**差分サイズに比例**する |
| リポジトリ全体への読み取り権限 | Claudeは`Read`ツール等でリポジトリ内の任意のファイルを読む能力自体は持っている。変更内容の理解のために「呼び出し元の確認」「`CLAUDE.md`の規約参照」など、必要と判断すれば追加でファイルを読みに行くことがあり、その分トークンは増える |
| コード取得方法 | 実行環境へのコード取得も「直近20コミットのみ」等の浅いクローン(shallow clone)で行われ、全履歴は取得しない設計 |

**結論**: 基本はPRの差分サイズに比例するが、変更が複雑でコードベース内の関連箇所への参照が多いほど、文脈理解のために読み込み範囲が広がり、トークン使用量が増える傾向がある。

### レビュー範囲を制御する設定例

```yaml
claude_args: |
  --max-turns 5                 # 思考・ツール呼び出しの往復回数に上限を設定
  --allowedTools "Read,Edit"    # 使えるツールを制限(Bashなど高コストな探索を禁止)
```

```yaml
on:
  pull_request:
    paths:
      - 'src/**'    # 特定のディレクトリの変更時のみ実行
```

プロンプト側で「差分のみをレビューし、コードベース全体を探索しないでください」と明示的に指示することも有効。

## 8. コストを抑える工夫

- **トリガー条件を絞る**: 「push毎」ではなく「PR作成時のみ」に限定する
- **対象範囲を絞る**: 重要なディレクトリ・ファイルのみレビュー対象にする、軽微な修正PRは除外する(上記7章の`paths`フィルタ等)
- **ツール・往復回数を制限する**: `--allowedTools`や`--max-turns`で、Claudeが探索できる範囲・回数に上限を設ける
- **モデルを使い分ける**: 全件Opusではなく、軽いチェックは安価なモデル(Sonnet等)を使う
- **利用上限(スペンドキャップ)を設定**: 月間の上限額を決め、超過を防ぐ(APIキー利用時、Anthropic Consoleで設定可能)
- **人間レビューとの役割分担**: AIレビューは一次チェックに留め、最終判断は人間が行うことで、必要以上に高精度・高頻度な実行を避ける

## 9. 導入検討時の論点(会社への提案ポイント)

1. **個人プランの範囲では大規模運用は難しい**: `CLAUDE_CODE_OAUTH_TOKEN`方式は個人の利用枠を共有するため、開発者本人の対話利用を圧迫する。組織として本格導入するなら、Team/Enterpriseプラン + 別途API予算の確保が現実的
2. **マージブロックはできない**: あくまで人間の判断を助ける「補助」であり、承認フローの代替にはならない。既存のレビュープロセスに追加する形での設計が必要
3. **費用は運用設計次第で大きく変動する**: トリガー条件・対象範囲・使用モデルの設計により、コストは数倍単位で変わる。本格導入前に、小規模なパイロット運用でコスト感を実測することを推奨
4. **公式Code ReviewサービスとGitHub Action自前構築、どちらが自社に合うか**: 手軽さを取るか、柔軟性・コストコントロールを取るかの検討が必要

---
*本資料は2026年8月時点の情報に基づく。料金・仕様は変更される可能性があるため、導入検討時はAnthropic公式サイトで最新情報を確認すること。*
